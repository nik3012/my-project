classdef (Abstract) bmmo_HocPoly2Spline < handle
% <help_update_needed>
%  for the class and for the function
%
% 
    
    properties
    end
    
    methods (Static, Abstract)
        cs = getOptions(CetSetpoint);
    end % methods
    
end %classdef